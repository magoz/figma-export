{
  "name": "figma-export",
  "version": "1.0.0",
  "description": "Figma plugin: export PNGs with real DPI metadata (pHYs), so prints come out at the right physical size",
  "repository": "github:magoz/figma-export",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.100.0",
    "typescript": "^5.6.0"
  }
}
